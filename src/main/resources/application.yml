server:
  port: 8088

logging:
  level:
    org.springframework: debug

spring:
  messages:
    basename: messages

management:
  endpoints:
    web:
      exposure:
        include: "*"